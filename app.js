angular.module("practiceApp").service("mainServ", function(){
// until we use something that applies here we will leave the second param the array blank
// if we use router we will eject something there but not for right now
// created an agular method with arguments in it to begin
// .controller();
// for modularity purposes it is good to keep these pages seperate and not put .controller() here
// hence go to mainCtrl to begin the next step

// we need to connect our controller to the service

this.test = "anything";
// a service method is a contructor function except we are not out to create a bunch of objects but it is sumilar to how we would reference a constructor funtion
// test is the variable caleld anything, if we want to access this test from our controller we would 



});
